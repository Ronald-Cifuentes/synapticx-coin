# DAG Ordering

Harness para conflictos de nullifier. **DAG no implementado**; cadena actual es lineal.

```bash
python simulations/dag-ordering/run_nullifier_conflict_simulator.py
```

Verifica: segundo bloque con mismo nullifier es rechazado (lineal). Define el caso que DAG debe resolver.
